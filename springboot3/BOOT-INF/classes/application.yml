spring:
  application:
    name: ruoyi-system
  config:
    activate:
      on-profile: dev
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          url: jdbc:mysql://mysql:3306/ry-vue-opc?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
          username: root
          password: Opc@2026!
  redis:
    host: redis
    port: 6379
    password: Opc@2026!

# ruoyi-system 使用 vanilla MyBatis（非 MyBatis-Plus），所以 mapper-locations 在 mybatis.* 下
mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.ruoyi.system.domain
  configuration:
    map-underscore-to-camel-case: true

jasypt:
  encryptor:
    password: OpcEncrypt!2026

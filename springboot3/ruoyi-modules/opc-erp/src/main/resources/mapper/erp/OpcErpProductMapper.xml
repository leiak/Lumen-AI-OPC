<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.erp.mapper.OpcErpProductMapper">

    <sql id="BaseColumns">
        id, company_id, sku_root, name, category, brand, unit, description,
        spec_attrs, status, created_by, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.erp.domain.OpcErpProduct">
        INSERT INTO opc_erp_product (
            <include refid="BaseColumns"/>
        )
        VALUES (
            #{id}, #{companyId}, #{skuRoot}, #{name}, #{category}, #{brand},
            #{unit}, #{description}, #{specAttrs}, #{status}, #{createdBy}, NOW(), NOW()
        )
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.erp.domain.OpcErpProduct">
        UPDATE opc_erp_product
        <set>
            <if test="skuRoot != null">sku_root = #{skuRoot},</if>
            <if test="name != null">name = #{name},</if>
            <if test="category != null">category = #{category},</if>
            <if test="brand != null">brand = #{brand},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="description != null">description = #{description},</if>
            <if test="specAttrs != null">spec_attrs = #{specAttrs},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <delete id="deleteById">
        DELETE FROM opc_erp_product
        WHERE id = #{id} AND company_id = #{companyId}
    </delete>

    <select id="selectById" resultType="com.ruoyi.opc.erp.domain.OpcErpProduct">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_product
        WHERE id = #{id} AND company_id = #{companyId}
    </select>

    <select id="selectList" resultType="com.ruoyi.opc.erp.domain.OpcErpProduct">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_product
        <where>
            company_id = #{companyId}
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                  OR sku_root LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countList" resultType="int">
        SELECT COUNT(1)
        FROM opc_erp_product
        <where>
            company_id = #{companyId}
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                  OR sku_root LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectListByCategory" resultType="com.ruoyi.opc.erp.domain.OpcErpProduct">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_product
        WHERE company_id = #{companyId} AND category = #{category}
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.erp.mapper.OpcErpPurchaseMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        <id property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="purchaseNo" column="purchase_no"/>
        <result property="supplierId" column="supplier_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="operatorId" column="operator_id"/>
        <result property="confirmedBy" column="confirmed_by"/>
        <result property="confirmedAt" column="confirmed_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="cancelledAt" column="cancelled_at"/>
        <result property="remark" column="remark"/>
        <result property="createdBy" column="created_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, company_id, purchase_no, supplier_id, total_amount, status,
        operator_id, confirmed_by, confirmed_at, completed_at, cancelled_at,
        remark, created_by, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        INSERT INTO opc_erp_purchase (
            <include refid="BaseColumns"/>
        )
        VALUES (
            #{id}, #{companyId}, #{purchaseNo}, #{supplierId}, #{totalAmount},
            #{status}, #{operatorId}, #{confirmedBy}, #{confirmedAt},
            #{completedAt}, #{cancelledAt}, #{remark}, #{createdBy},
            NOW(), NOW()
        )
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        UPDATE opc_erp_purchase
        <set>
            <if test="purchaseNo != null">purchase_no = #{purchaseNo},</if>
            <if test="supplierId != null">supplier_id = #{supplierId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="operatorId != null">operator_id = #{operatorId},</if>
            <if test="confirmedBy != null">confirmed_by = #{confirmedBy},</if>
            <if test="confirmedAt != null">confirmed_at = #{confirmedAt},</if>
            <if test="completedAt != null">completed_at = #{completedAt},</if>
            <if test="cancelledAt != null">cancelled_at = #{cancelledAt},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <delete id="deleteById">
        DELETE FROM opc_erp_purchase
        WHERE id = #{id} AND company_id = #{companyId}
    </delete>

    <select id="selectById" resultType="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_purchase
        WHERE id = #{id} AND company_id = #{companyId}
    </select>

    <select id="selectByPurchaseNo" resultType="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_purchase
        WHERE company_id = #{companyId} AND purchase_no = #{purchaseNo}
    </select>

    <select id="selectList" resultType="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_purchase
        <where>
            company_id = #{companyId}
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="supplierId != null">
                AND supplier_id = #{supplierId}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countList" resultType="int">
        SELECT COUNT(1)
        FROM opc_erp_purchase
        <where>
            company_id = #{companyId}
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="supplierId != null">
                AND supplier_id = #{supplierId}
            </if>
        </where>
    </select>

    <select id="selectListByStatus" resultType="com.ruoyi.opc.erp.domain.OpcErpPurchase">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_purchase
        WHERE company_id = #{companyId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE opc_erp_purchase
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <select id="countTodayPurchases" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM opc_erp_purchase
        WHERE company_id = #{companyId}
          AND DATE(create_time) = #{today}
    </select>

</mapper>

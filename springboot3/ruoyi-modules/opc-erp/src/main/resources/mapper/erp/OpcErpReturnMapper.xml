<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.erp.mapper.OpcErpReturnMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.opc.erp.domain.OpcErpReturn">
        <id property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="returnNo" column="return_no"/>
        <result property="returnType" column="return_type"/>
        <result property="refId" column="ref_id"/>
        <result property="refundAmount" column="refund_amount"/>
        <result property="status" column="status"/>
        <result property="operatorId" column="operator_id"/>
        <result property="confirmedBy" column="confirmed_by"/>
        <result property="confirmedAt" column="confirmed_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="reason" column="reason"/>
        <result property="remark" column="remark"/>
        <result property="createdBy" column="created_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, company_id, return_no, return_type, ref_id, refund_amount, status,
        operator_id, confirmed_by, confirmed_at, completed_at,
        reason, remark, created_by, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.erp.domain.OpcErpReturn">
        INSERT INTO opc_erp_return (
            <include refid="BaseColumns"/>
        )
        VALUES (
            #{id}, #{companyId}, #{returnNo}, #{returnType}, #{refId}, #{refundAmount},
            #{status}, #{operatorId}, #{confirmedBy}, #{confirmedAt}, #{completedAt},
            #{reason}, #{remark}, #{createdBy}, NOW(), NOW()
        )
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.erp.domain.OpcErpReturn">
        UPDATE opc_erp_return
        <set>
            <if test="returnNo != null">return_no = #{returnNo},</if>
            <if test="returnType != null">return_type = #{returnType},</if>
            <if test="refId != null">ref_id = #{refId},</if>
            <if test="refundAmount != null">refund_amount = #{refundAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="operatorId != null">operator_id = #{operatorId},</if>
            <if test="confirmedBy != null">confirmed_by = #{confirmedBy},</if>
            <if test="confirmedAt != null">confirmed_at = #{confirmedAt},</if>
            <if test="completedAt != null">completed_at = #{completedAt},</if>
            <if test="reason != null">reason = #{reason},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <delete id="deleteById">
        DELETE FROM opc_erp_return
        WHERE id = #{id} AND company_id = #{companyId}
    </delete>

    <select id="selectById" resultType="com.ruoyi.opc.erp.domain.OpcErpReturn">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_return
        WHERE id = #{id} AND company_id = #{companyId}
    </select>

    <select id="selectByReturnNo" resultType="com.ruoyi.opc.erp.domain.OpcErpReturn">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_return
        WHERE company_id = #{companyId} AND return_no = #{returnNo}
    </select>

    <select id="selectList" resultType="com.ruoyi.opc.erp.domain.OpcErpReturn">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_return
        <where>
            company_id = #{companyId}
            <if test="returnType != null and returnType != ''">
                AND return_type = #{returnType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countList" resultType="int">
        SELECT COUNT(1)
        FROM opc_erp_return
        <where>
            company_id = #{companyId}
            <if test="returnType != null and returnType != ''">
                AND return_type = #{returnType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectListByType" resultType="com.ruoyi.opc.erp.domain.OpcErpReturn">
        SELECT <include refid="BaseColumns"/>
        FROM opc_erp_return
        WHERE company_id = #{companyId} AND return_type = #{returnType}
        ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE opc_erp_return
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <!-- Task 6: 当日退货单计数(return_no 序号生成) -->
    <select id="countTodayReturns" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM opc_erp_return
        WHERE company_id = #{companyId}
          AND DATE(create_time) = #{today}
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.hr.mapper.OpcHrJobMapper">

    <sql id="BaseColumns">
        id, company_id, created_by, title, category, description, full_jd, skills_json,
        salary_min, salary_max, location, status, publish_at, close_at,
        create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.hr.domain.OpcHrJob">
        INSERT INTO opc_hr_job (id, company_id, created_by, title, category, description,
                                full_jd, skills_json, salary_min, salary_max, location,
                                status, publish_at, close_at)
        VALUES (#{id}, #{companyId}, #{createdBy}, #{title}, #{category}, #{description},
                #{fullJd}, #{skillsJson}, #{salaryMin}, #{salaryMax}, #{location},
                #{status}, #{publishAt}, #{closeAt})
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.hr.domain.OpcHrJob">
        UPDATE opc_hr_job
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="category != null">category = #{category},</if>
            <if test="description != null">description = #{description},</if>
            <if test="fullJd != null">full_jd = #{fullJd},</if>
            <if test="skillsJson != null">skills_json = #{skillsJson},</if>
            <if test="salaryMin != null">salary_min = #{salaryMin},</if>
            <if test="salaryMax != null">salary_max = #{salaryMax},</if>
            <if test="location != null">location = #{location},</if>
            <if test="status != null">status = #{status},</if>
            <if test="publishAt != null">publish_at = #{publishAt},</if>
            <if test="closeAt != null">close_at = #{closeAt},</if>
        </set>
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <delete id="deleteById">
        DELETE FROM opc_hr_job
        WHERE id = #{id} AND company_id = #{companyId} AND status = 'DRAFT'
    </delete>

    <select id="selectById" resultType="com.ruoyi.opc.hr.domain.OpcHrJob">
        SELECT <include refid="BaseColumns" />
        FROM opc_hr_job
        WHERE id = #{id} AND company_id = #{companyId}
    </select>

    <select id="selectList" resultType="com.ruoyi.opc.hr.domain.OpcHrJob">
        SELECT <include refid="BaseColumns" />
        FROM opc_hr_job
        <where>
            company_id = #{companyId}
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="int">
        SELECT COUNT(1)
        FROM opc_hr_job
        <where>
            company_id = #{companyId}
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>

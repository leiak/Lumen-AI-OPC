<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.hr.mapper.OpcHrInterviewMapper">

    <sql id="BaseColumns">
        id, company_id, application_id, round, type, interviewer_id,
        scheduled_at, duration_min, feedback, result, create_time
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.hr.domain.OpcHrInterview">
        INSERT INTO opc_hr_interview (id, company_id, application_id, round, type, interviewer_id,
                                       scheduled_at, duration_min, feedback, result)
        VALUES (#{id}, #{companyId}, #{applicationId}, #{round}, #{type}, #{interviewerId},
                #{scheduledAt}, #{durationMin}, #{feedback}, #{result})
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.hr.domain.OpcHrInterview">
        UPDATE opc_hr_interview
        <set>
            <if test="type != null and type != ''">type = #{type},</if>
            <if test="interviewerId != null">interviewer_id = #{interviewerId},</if>
            <if test="scheduledAt != null">scheduled_at = #{scheduledAt},</if>
            <if test="durationMin != null">duration_min = #{durationMin},</if>
            <if test="feedback != null">feedback = #{feedback},</if>
            <if test="result != null">result = #{result},</if>
        </set>
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <select id="selectById" resultType="com.ruoyi.opc.hr.domain.OpcHrInterview">
        SELECT <include refid="BaseColumns" />
        FROM opc_hr_interview
        WHERE id = #{id} AND company_id = #{companyId}
    </select>

    <select id="selectListByApplication" resultType="com.ruoyi.opc.hr.domain.OpcHrInterview">
        SELECT <include refid="BaseColumns" />
        FROM opc_hr_interview
        WHERE company_id = #{companyId} AND application_id = #{applicationId}
        ORDER BY round ASC
    </select>

    <select id="maxRoundByApplication" resultType="java.lang.Integer">
        SELECT MAX(round) FROM opc_hr_interview
        WHERE application_id = #{applicationId}
    </select>
</mapper>

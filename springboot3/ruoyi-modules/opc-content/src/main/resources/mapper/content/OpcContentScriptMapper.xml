<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.content.mapper.OpcContentScriptMapper">

    <resultMap id="ScriptResultMap" type="com.ruoyi.opc.content.domain.OpcContentScript">
        <id property="id" column="id" />
        <result property="companyId" column="company_id" />
        <result property="userId" column="user_id" />
        <result property="type" column="type" />
        <result property="title" column="title" />
        <result property="promptInput" column="prompt_input" />
        <result property="contentJson" column="content_json" />
        <result property="contentMd" column="content_md" />
        <result property="wordCount" column="word_count" />
        <result property="status" column="status" />
        <result property="sourceScriptId" column="source_script_id" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="BaseColumns">
        id, company_id, user_id, type, title, prompt_input, content_json, content_md,
        word_count, status, source_script_id, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.content.domain.OpcContentScript">
        INSERT INTO opc_content_script
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="companyId != null">company_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="type != null">type,</if>
            <if test="title != null">title,</if>
            <if test="promptInput != null">prompt_input,</if>
            <if test="contentJson != null">content_json,</if>
            <if test="contentMd != null">content_md,</if>
            <if test="wordCount != null">word_count,</if>
            <if test="status != null">status,</if>
            <if test="sourceScriptId != null">source_script_id,</if>
            <if test="createdAt != null">created_at,</if>
            <if test="updatedAt != null">updated_at,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="companyId != null">#{companyId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="type != null">#{type},</if>
            <if test="title != null">#{title},</if>
            <if test="promptInput != null">#{promptInput},</if>
            <if test="contentJson != null">#{contentJson},</if>
            <if test="contentMd != null">#{contentMd},</if>
            <if test="wordCount != null">#{wordCount},</if>
            <if test="status != null">#{status},</if>
            <if test="sourceScriptId != null">#{sourceScriptId},</if>
            <if test="createdAt != null">#{createdAt},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.content.domain.OpcContentScript">
        UPDATE opc_content_script
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="title != null">title = #{title},</if>
            <if test="promptInput != null">prompt_input = #{promptInput},</if>
            <if test="contentJson != null">content_json = #{contentJson},</if>
            <if test="contentMd != null">content_md = #{contentMd},</if>
            <if test="wordCount != null">word_count = #{wordCount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sourceScriptId != null">source_script_id = #{sourceScriptId},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <update id="softDeleteById">
        UPDATE opc_content_script
        SET status = 'DELETED', updated_at = NOW()
        WHERE id = #{id} AND company_id = #{companyId}
    </update>

    <select id="selectById" resultMap="ScriptResultMap">
        SELECT <include refid="BaseColumns" />
        FROM opc_content_script
        WHERE id = #{id} AND company_id = #{companyId} AND status != 'DELETED'
    </select>

    <select id="selectList" resultMap="ScriptResultMap">
        SELECT <include refid="BaseColumns" />
        FROM opc_content_script
        <where>
            company_id = #{companyId}
            AND status != 'DELETED'
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="int">
        SELECT COUNT(1)
        FROM opc_content_script
        <where>
            company_id = #{companyId}
            AND status != 'DELETED'
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="countTodayByCompany" resultType="int">
        SELECT COUNT(1)
        FROM opc_content_script
        WHERE company_id = #{companyId}
          AND created_at &gt;= CURDATE()
          AND created_at &lt; DATE_ADD(CURDATE(), INTERVAL 1 DAY)
          AND status != 'DELETED'
    </select>

    <select id="countByStatus" resultType="int">
        SELECT COUNT(1)
        FROM opc_content_script
        WHERE company_id = #{companyId} AND status = #{status}
    </select>

    <select id="countFailedLast7Days" resultType="int">
        SELECT COUNT(1)
        FROM opc_content_script
        WHERE company_id = #{companyId}
          AND status = 'FAILED'
          AND created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
    </select>

    <select id="selectRecent" resultMap="ScriptResultMap">
        SELECT <include refid="BaseColumns" />
        FROM opc_content_script
        WHERE company_id = #{companyId} AND status != 'DELETED'
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
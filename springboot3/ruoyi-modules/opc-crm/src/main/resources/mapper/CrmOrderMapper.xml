<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.opc.crm.mapper.CrmOrderMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.opc.crm.domain.CrmOrder">
        <id property="id" column="id" />
        <result property="customerId" column="customer_id" />
        <result property="contractId" column="contract_id" />
        <result property="orderNo" column="order_no" />
        <result property="itemsJson" column="items_json" />
        <result property="total" column="total" />
        <result property="status" column="status" />
        <result property="paidAt" column="paid_at" />
        <result property="shippedAt" column="shipped_at" />
        <result property="remark" column="remark" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
        <result property="deleted" column="deleted" />
    </resultMap>

    <sql id="BaseColumns">
        id, customer_id, contract_id, order_no, items_json, total, status,
        paid_at, shipped_at, remark,
        create_by, create_time, update_by, update_time, deleted
    </sql>

    <insert id="insert" parameterType="com.ruoyi.opc.crm.domain.CrmOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO opc_crm_order (
            customer_id, contract_id, order_no, items_json, total, status,
            paid_at, shipped_at, remark,
            create_by, create_time, update_by, update_time, deleted
        ) VALUES (
            #{customerId}, #{contractId}, #{orderNo}, #{itemsJson}, #{total}, #{status},
            #{paidAt}, #{shippedAt}, #{remark},
            #{createBy}, NOW(), #{updateBy}, NOW(), #{deleted}
        )
    </insert>

    <update id="updateById" parameterType="com.ruoyi.opc.crm.domain.CrmOrder">
        UPDATE opc_crm_order SET
            customer_id = #{customerId}, contract_id = #{contractId}, items_json = #{itemsJson},
            total = #{total}, status = #{status}, paid_at = #{paidAt}, shipped_at = #{shippedAt},
            remark = #{remark},
            deleted = #{deleted}, update_by = #{updateBy}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" />
        FROM opc_crm_order
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCustomerId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" />
        FROM opc_crm_order
        WHERE customer_id = #{customerId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByContractId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" />
        FROM opc_crm_order
        WHERE contract_id = #{contractId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="countByOrderNo" resultType="int">
        SELECT COUNT(*) FROM opc_crm_order
        WHERE order_no = #{orderNo}
    </select>

</mapper>

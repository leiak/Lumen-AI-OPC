<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.ruoyi</groupId>
        <artifactId>ruoyi-modules</artifactId>
        <version>3.6.8</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>opc-insight</artifactId>

    <description>
        OPC 数据洞察 Agent（M4 INSIGHT MVP）
    </description>

    <dependencies>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-common-datasource</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-common-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-common-log</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-common-swagger</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-api-system</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>opc-common</artifactId>
        </dependency>
        <!-- 远程调用 AI 中台 -->
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>opc-ai-core</artifactId>
        </dependency>

        <!-- M4 INSIGHT：Feign 客户端需要引用对端的 VO/domain 类（编译期）。运行时由 Feign 拉 JSON
             反序列化为这些类的实例，所以类必须在 runtime classpath 可达 —— compile scope。
             之所以不能 pure Feign：opc-finance 返回 R<OpcFinanceTaxReport>（domain 类），
             opc-user-center 返回 R<OpcCompanyProfile>（domain 类）—— Feign 反序列化需要类在 classpath。 -->
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>opc-finance</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>opc-billing</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>opc-user-center</artifactId>
        </dependency>

        <!-- ================ 测试依赖 ================ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <!-- 禁用 repackage — 否则会把所有依赖打进 BOOT-INF/lib 形成 fat jar,
                             里面嵌套的 opc-finance-3.6.8.jar 等会带旧版 mapper XML(裸 < 字符),
                             MyBatis 启动时 SAXParseException。改为独立 jar + target/dependency/*.jar
                             由 Dockerfile 用 -cp 组装 classpath,Dockerfile 已切换为这种 thin-jar 模式。 -->
                        <phase>none</phase>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
